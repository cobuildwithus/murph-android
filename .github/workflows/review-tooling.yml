name: Review Tooling

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: review-tooling-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24.14.1
      - name: Install review tooling
        run: |
          corepack enable
          pnpm install --frozen-lockfile --ignore-scripts
      - name: Verify review workflow
        run: |
          pnpm review:verify
          pnpm review:gpt --list-presets
